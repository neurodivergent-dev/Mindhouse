{
  "name": "search",
  "description": "Search once with a focused query describing what the code does or its name.\n\nWrite queries using function/class names or behavior descriptions, not error messages.\nReturns file paths and line numbers — navigate directly there, do not repeat the search.\nPass a git URL or local path as `repo`; indexes are cached for the session.\n",
  "inputSchema": {
    "properties": {
      "query": {
        "description": "Natural language or code query.",
        "title": "Query",
        "type": "string"
      },
      "repo": {
        "description": "A local directory path or https:// or http:// git URL (e.g. https://github.com/org/repo) to index and search. The index is cached after the first call, so repeat queries are fast.",
        "title": "Repo",
        "type": "string"
      },
      "top_k": {
        "default": 5,
        "description": "Number of results to return.",
        "minimum": 1,
        "title": "Top K",
        "type": "integer"
      },
      "max_snippet_lines": {
        "anyOf": [
          {
            "minimum": 0,
            "type": "integer"
          },
          {
            "type": "null"
          }
        ],
        "default": 10,
        "description": "Lines of source to include per result. Default (10): function/class signature + first body lines, enough to confirm the location. 0: file path and line range only. None: full chunk (~10-20 lines). If the snippet does not contain enough context to confirm you have the right location, call again with max_snippet_lines=None.",
        "title": "Max Snippet Lines"
      }
    },
    "required": [
      "query",
      "repo"
    ],
    "title": "searchArguments",
    "type": "object"
  }
}