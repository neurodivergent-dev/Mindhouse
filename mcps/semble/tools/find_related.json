{
  "name": "find_related",
  "description": "Find code similar to a known location.\n\nUseful for discovering all implementations of an interface, all callers of a function,\nor all tests for a class. Use after `search` when you need related code beyond the primary result.\nPass `file_path` and `line` from a prior search result.\n",
  "inputSchema": {
    "properties": {
      "file_path": {
        "description": "Path to the file as stored in the index (use file_path from a search result).",
        "title": "File Path",
        "type": "string"
      },
      "line": {
        "description": "Line number (1-indexed).",
        "title": "Line",
        "type": "integer"
      },
      "repo": {
        "description": "A local directory path or https:// or http:// git URL (e.g. https://github.com/org/repo) to index and search. The index is cached after the first call, so repeat queries are fast.",
        "title": "Repo",
        "type": "string"
      },
      "top_k": {
        "default": 5,
        "description": "Number of similar chunks to return.",
        "minimum": 1,
        "title": "Top K",
        "type": "integer"
      },
      "max_snippet_lines": {
        "anyOf": [
          {
            "minimum": 0,
            "type": "integer"
          },
          {
            "type": "null"
          }
        ],
        "default": 10,
        "description": "Lines of source per result. Default 10 = signature + first body lines. 0 = location only. None = full chunk.",
        "title": "Max Snippet Lines"
      }
    },
    "required": [
      "file_path",
      "line",
      "repo"
    ],
    "title": "find_relatedArguments",
    "type": "object"
  }
}